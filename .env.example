# Go Flow OSS Edition - Environment Configuration
# Copy this file to .env and customize as needed.

# ==========================================
# Edition (do not change for OSS)
# ==========================================
EDITION=oss
NEXT_PUBLIC_EDITION=oss

# ==========================================
# Database
# ==========================================
# MongoDB connection string (pre-configured for Docker Compose)
DATABASE_URL=mongodb://mongodb:27017/goflow?replicaSet=rs0&directConnection=true

# ==========================================
# URLs
# ==========================================
# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
# Backend API URL (for frontend to connect)
NEXT_PUBLIC_API_URL=http://localhost:3001

# ==========================================
# Security (auto-generated if not set)
# ==========================================
# JWT_SECRET=your-secret-key-at-least-32-characters-long
# SESSION_SECRET=your-session-secret-at-least-32-characters-long

# ==========================================
# Authentication (OSS)
# ==========================================
# OSS uses local password-based auth (no OAuth needed).
# On first run a default user is created automatically.
# Set a custom password here or one will be auto-generated.
# OSS_USER_PASSWORD=your-password

# ==========================================
# Local Storage
# ==========================================
# Directory for uploaded files (inside Docker container)
LOCAL_UPLOAD_DIR=/data/uploads

# ==========================================
# AI API Keys (optional — for AI-powered features)
# ==========================================
# OpenAI API key (for GPT-based features)
# OPENAI_API_KEY=sk-...
# Anthropic API key (for Claude-based features)
# ANTHROPIC_API_KEY=sk-ant-...
