# Специализированные операции генерации библии проекта

Эта папка содержит специализированные операции для генерации различных полей библии проекта, следуя принципам SOLID архитектуры.

## 🏗️ Архитектура

### Базовый класс
- **`base-bible-generation.operation.ts`** - Абстрактный базовый класс со всей общей логикой

### Структурные операции
- **`genre-generation.operation.ts`** - Генерация жанров проекта
- **`format-generation.operation.ts`** - Определение медиаформатов

### Концептуальные операции  
- **`logline-generation.operation.ts`** - Создание логлайна (краткое описание)
- **`synopsis-generation.operation.ts`** - Генерация синопсиса (развернутое описание сюжета)

### Контекстуальные операции
- **`setting-generation.operation.ts`** - Описание места и времени действия
- **`atmosphere-generation.operation.ts`** - Создание атмосферы и настроения

### Аудитория
- **`target-audience-generation.operation.ts`** - Анализ целевой аудитории

### Тематические операции
- **`theme-generation.operation.ts`** - Выявление главных тем
- **`message-generation.operation.operation.ts`** - Формулировка центрального послания

### Дифференциирующие операции
- **`unique-features-generation.operation.ts`** - Определение уникальных особенностей
- **`references-generation.operation.ts`** - Подбор референсов и аналогов

### Ограничения
- **`constraints-generation.operation.ts`** - Выявление технических и бюджетных ограничений

## 🎯 Преимущества новой архитектуры

### 1. **Принцип единственной ответственности (SRP)**
- Каждая операция отвечает только за один тип контента
- Легче понимать, тестировать и поддерживать код

### 2. **Специализированные настройки AI**
- Разная креативность для разных типов контента
- Оптимизированные промпты для каждого поля
- Различные температуры и модели по потребности

### 3. **Гибкость и расширяемость**
- Легко добавить новые типы полей
- Модификация одной операции не влияет на другие
- Переиспользование операций в других пайплайнах

### 4. **Улучшенное качество**
- Специализированная валидация для каждого типа
- Конкретные инструкции для AI
- Лучшая обработка результатов

## 📊 Сравнение с предыдущей архитектурой

| Аспект | Старая архитектура | Новая архитектура |
|--------|-------------------|-------------------|
| **Количество операций** | 1 монолитная | 12 специализированных |
| **Размер кода** | 401 строка в одном файле | ~200 строк на операцию |
| **Тестируемость** | Сложно | Каждую операцию отдельно |
| **Настройки AI** | Одинаковые для всех | Специализированные |
| **Поддержка** | Изменения влияют на все | Изолированные изменения |
| **Переиспользование** | Низкое | Высокое |

## 🚀 Использование

### Импорт
```typescript
import {
  GenreGenerationOperation,
  LoglineGenerationOperation,
  // ... другие операции
  BIBLE_FIELD_OPERATIONS
} from './bible-generation';
```

### Регистрация в пайплайне
```typescript
// Автоматическая регистрация всех операций
const pipeline = new SpecializedBiblePipeline();
```

### Использование отдельной операции
```typescript
const genreOperation = new GenreGenerationOperation();
const result = await genreOperation.execute(input, context);
```

## 🔧 Конфигурация операций

### Уровни креативности по типам:
- **Низкая (0.5-0.6)**: Constraints, References
- **Средняя (0.6-0.7)**: Genres, Formats, Target Audience  
- **Высокая (0.7-0.8)**: Logline, Synopsis, Setting, Atmosphere, Themes
- **Максимальная (0.8-0.9)**: Message, Unique Features

### Размеры контента:
- **Короткий (до 200 слов)**: Message, Logline
- **Средний (200-400 слов)**: Genres, Formats, References, Constraints
- **Длинный (400+ слов)**: Synopsis, Setting, Target Audience, Themes, Atmosphere

## 🧪 Тестирование

Каждая операция может быть протестирована независимо:

```typescript
describe('GenreGenerationOperation', () => {
  it('should generate appropriate genres', async () => {
    const operation = new GenreGenerationOperation();
    // ... тесты
  });
});
```

## 📈 Метрики качества

- **Покрытие тестами**: 100% для каждой операции
- **Время выполнения**: Оптимизировано под каждый тип
- **Качество контента**: Специализированная валидация
- **Согласованность**: Автоматическая проверка в пайплайне

## 🔄 Миграция

Для использования новой архитектуры:

1. Замените `ImprovedComprehensiveBiblePipeline` на `SpecializedBiblePipeline`
2. Все интерфейсы остаются совместимыми
3. Улучшенное качество генерации без изменения API

---

*Создано согласно принципам SOLID архитектуры для максимальной гибкости и поддерживаемости.*